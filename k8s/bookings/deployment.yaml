apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookings-service
  namespace: cinema-app
  labels:
    app: bookings-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookings-service
  template:
    metadata:
      labels:
        app: bookings-service
    spec:
      containers:
        - name: bookings-service
          image: ghcr.io/linaan0/cinema-app-bookings:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: bookings-config
            - secretRef:
                name: jwt-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 25
            periodSeconds: 10
